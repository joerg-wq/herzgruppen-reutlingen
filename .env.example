# Optional: finale Domain ohne trailing slash. OG-Tags & canonical nutzen dann diesen Wert.
# Solange leer bzw. unset: Fallback in client/src/config.ts (Cloudflare Pages).
# VITE_SITE_URL=https://ihre-domain.de
